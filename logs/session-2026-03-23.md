# Session Log — March 23, 2026

## Summary
Continuation session. Resumed from a prior context where the full 13-trip travel system
had been built. This session focused on airport selection logic and flight priority rules.

---

## Changes Made

### 1. Remove EWR as preferred NYC airport
**Commits:** `437b893`
**Files:** `travel-system.md`, all 6 home game trip files (trips 1, 2, 5, 6, 9, 10)

**Problem:** Every home game file and travel-system.md was labeling EWR as the preferred
or best NYC airport, with notes like "EWR preferred — avoids NYC traffic via NJ Tpke → I-87 N."

**Fix:** Removed all preference language. All three airports (EWR, LGA, JFK) are now listed
as equal options. Flight searches should compare all three. Route info updated to show
driving distances/times from each airport to the Central Valley hotel area.

---

### 2. Apply nonstop+lowest-price rule to ALL fly trips
**Commits:** `ea0cd1c`, `000c083`
**Files:** `CLAUDE.md`, `travel-system.md`, trips 3 (Temple) and 8 (Memphis)

**Problem:** The nonstop-first, lowest-price rule was only explicitly stated for home games.
Away fly trips (Temple, Memphis) had softer language that could allow EWR-style
defaulting to a preferred airline.

**Fix:** Rule now explicitly applies to ALL fly trips. Selection priority:
1. Nonstop only
2. Cheapest nonstop wins regardless of airline
3. American as tiebreaker on equal price only

---

### 3. Add Step 0 session orientation
**Commits:** `a07f7c3`
**Files:** `CLAUDE.md`

Added mandatory Step 0 with 4 sub-steps that run at the start of every session:
- 0a: Read latest session log
- 0b: Check today's date against game schedule
- 0c: Sweep all 13 trip files for status
- 0d: Deliver session briefing before any other action

---

### 4. Standardize trip status lines
**Commits:** `2673d04`
**Files:** All 13 trip files

Standardized line 2 of every trip file to use consistent format:
`**Status: [STATUS] — [details]**`
Required for Step 0 parsing to work correctly.

---

## Session 2 — Technical Audit & Structural Improvements

### 5. Full technical audit conducted
Comprehensive audit covering architecture, code organization, security, reliability,
testability, and developer experience. Overall rating: 6.5/10.

Top 5 findings:
1. API key exposed during audit (SerpAPI key in .env was read by subagent)
2. No automated validation of trip files
3. `get_flights()` error handling too generic
4. No README explaining the architecture
5. No SerpAPI quota tracking

---

### 6. Rotate SerpAPI key + deny Read(.env)
**Files:** `.claude/settings.local.json`, `.env` (user action)

API key was exposed when audit subagent read `.env`. User rotated the key.
Added `deny: ["Read(.env)"]` to settings.local.json to prevent future exposure.

---

### 7. Improve get_flights() error handling
**Commit:** `8e45005`
**Files:** `flights.py`

Replaced single catch-all exception handler with distinct error types:
- Network timeout
- Connection failure
- HTTP 401 (bad key)
- HTTP 429 (quota exhausted)
- HTTP 5xx (server error)
- JSON parse failure
- SerpAPI JSON body errors (quota/credit messages)
- Empty results (now explicitly says "do not fabricate")

---

### 8. Add README.md
**Commit:** `32e5d78`
**Files:** `README.md` (new)

Created entry-point documentation explaining:
- CLAUDE.md is the executable (not a traditional program)
- Setup instructions
- Game schedule table
- Line-2 status convention with warning not to edit manually
- Key rules summary
- Secrets handling note

---

### 9. Add SerpAPI quota tracking
**Commit:** `3fb659b`
**Files:** `flights.py`, `.gitignore`

Tracks API call count in `.serpapi_usage.json` (gitignored, auto-resets monthly).
- Warns at 80/100 searches used
- Blocks at 100 (before hitting API, no wasted call)
- Appends usage note to every successful result set for session log visibility
Current usage: 5/100 searches used this month.

---

### 10. Add validate_trips.py
**Commit:** `25d6fe0`
**Files:** `validate_trips.py` (new)

Structural validator for all 13 trip files. Checks:
- All 13 expected files exist
- Status line (line 2) uses a recognized prefix
- Required sections present (PLANNED/BOOKED trips — STUBs exempt)

First run revealed 6 failures: trips 1, 2, 5, 6, 9, 10 all missing RULES COMPLIANCE CHECK section.

---

### 11. Fix missing RULES COMPLIANCE CHECK in 6 home game trips
**Commit:** `cefd2de`
**Files:** trips 1, 2, 5, 6, 9, 10

Added standard RULES COMPLIANCE CHECK section to all 6 home game trip files.
Validator now passes 13/13.

---

### 12. Fix ghost EWR preference in ground transport
**Commits:** `34d67ec`, `6c3d926`
**Files:** trips 2, 5, 6, 9, 10; `travel-system.md`; `validate_trips.py`

**Problem (third recurrence):** Despite rule-level fixes, EWR was still hardcoded in
ground transport sections of 5 home game trip files and in Army-Navy get_flights
instructions in travel-system.md.

**Root cause:** Trip files contained EWR-specific content that acted as implicit
templates when new trips were planned. Rule layer was clean; data layer was not.

**Fix:**
- Updated ground transport in trips 2, 5, 6, 9, 10 to show directions from all
  three airports (matching Trip 1's correct format)
- Fixed Army-Navy instructions to call get_flights on EWR/LGA/JFK, not just EWR
- Added EWR ghost check to validator (later removed — see below)

**Follow-up:** User correctly noted the check was unnecessary once root cause was
removed. Cheapest-nonstop rule provides no mechanism for EWR to re-emerge.
Check removed in commit `6c3d926`.

---

### 13. Fix permissions — eliminate routine prompts
**Files:** `.claude/settings.local.json`

Changed `defaultMode` from `"acceptEdits"` to `"dontAsk"`. Previous setting was
causing prompts on Bash commands starting with `cd &&`, WebFetch, and subagents.
The `deny: ["Read(.env)"]` rule still protects the API key regardless of defaultMode.

---

### 14. Add departure time rules
**Commit:** `9ca9568`
**Files:** `CLAUDE.md`, `travel-system.md`

Added two new default rules to MASTER SETTINGS and FLIGHT DATA RULES:
- **Departure date:** Day before the game (already existed — confirmed)
- **Departure time:** Early morning preferred; no departures after 3:00 PM CT
Both are user-overridable.

---

### 15. Set bypassPermissions globally
**Files:** `~/.claude/settings.json`

User wanted no permission prompts on any tool calls. Set `permissions.defaultMode`
to `"bypassPermissions"` in global user settings. Confirmed working.

---

## Open Items
- None identified this session

## Booking Status
- Trip 1 (Bryant): PARTIALLY BOOKED — Hotel confirmed (Comfort Suites Central Valley); flights and rental car not yet booked
- Trips 2–12: PLANNED
- Trip 13 (Bowl): STUB — awaiting bowl assignment December 2026

---

### 17. Dashboard (index.html) — series of display fixes
**Commits:** `e97fab7`, `b92d6a8`, `445e1c2`, `0db1ae6`
**Files:** `index.html`

Four rounds of fixes to the web dashboard:

**Opponent name parsing (`e97fab7`)**
`parseTrip()` was leaving em-dashes and `@`/`vs.` prefixes in the opponent field
(e.g. `— @ Louisiana Tech Bulldogs`). Fixed regex chain to strip `—`/`–`, leading
`vs.`/`@`/`at`, parentheticals like `(HOME)`, and trailing emoji annotations like
`★ ARMY-NAVY GAME` and `⚡ FRIDAY NIGHT`.

**Modal ASCII art cleanup (`e97fab7`)**
Added `cleanTripPlan()` to strip box-drawing characters (`═══`, `─────`, `┌─┐`,
`│...│`, `└─┘`) and convert all-caps section headers (`GAME DETAILS\n────`) to
markdown `### headings` before rendering. Added `renderStatusBlock()` to format
the `**Status: TYPE — detail | detail**` line as a styled badge with per-component
bullet points (✅/❌) instead of a run-on bold sentence.

**Key-value row formatting (`b92d6a8`)**
All plan sections (Game Details, Hotel Options, Cost Summary, etc.) were rendering
as walls of text — every `Key     : Value` pair in one `<p>` block with `<br>`
separators, same color, same font. Added kv-row detection to `renderMarkdown()`:
lines matching `  Key    : Value` (2+ leading spaces, word-char key, whitespace
before colon) are converted to `<div class="kv-row">` with key in dim 10px
monospace and value in white 13px. Added `div` to the block-element exception list
so kv blocks aren't re-wrapped in `<p>`.

**Label rename + stat split (`445e1c2`)**
"Planned" was too close to "Booked" and the header showed a confusing "0 / 12 / 13".
Renamed to "Open" (nothing confirmed yet) with "In Progress" for partially booked.
Header stats split into four distinct counts: Booked / In Progress / Open / Total.
Section group headings updated to match. Badge labels updated on cards.

**Orphaned content and note formatting (`0db1ae6`)**
Three issues in modal content:
- Box values wrapping across two `│...│` rows produced orphaned continuation lines
  (e.g. `Notes : Morning arrival — full afternoon to` + orphan `drive to Central
  Valley and settle in`). Fixed by merging continuation lines onto the preceding
  kv value in `cleanTripPlan()`.
- Option labels (`Option 1 — American nonstop...`, `Option 2 — United nonstop...`)
  rendered as unstyled plain paragraphs. Fixed by converting to `**bold**` in
  `cleanTripPlan()` so they render as distinct bold subheadings.
- Note bullets (`  - text`) had 2 leading spaces that the list regex didn't handle.
  Fixed by changing `^[\-\*] ` to `^ *[\-\*] `. Multi-line bullet continuations
  (4+ space indent) are now also merged onto the preceding bullet.

---

### 16. Redefine BOOKED status — all applicable components required
**Files:** `CLAUDE.md`, `output-template.md`, `travel-system.md`, `trips/trip-1-bryant.md`

**Change:** BOOKED now requires ALL applicable components confirmed:
- Fly trips: flight + hotel + rental car
- Drive trips: hotel only (no flight or rental car needed)

Previously, "BOOKED" was used loosely for any confirmed reservation. Under the new definition,
a trip with only the hotel confirmed is PARTIALLY BOOKED.

**Impact:**
- `CLAUDE.md` Step 0c: Added PARTIALLY BOOKED to status legend; updated BOOKED definition
- `CLAUDE.md` automatic behaviors: Clarified exactly when to use BOOKED vs PARTIALLY BOOKED
- `output-template.md`: Added BOOKING STATUS DEFINITIONS table
- `travel-system.md`: Updated Trip 1 home game note to reflect "partially booked"
- `trips/trip-1-bryant.md`: Status changed from BOOKED → PARTIALLY BOOKED
